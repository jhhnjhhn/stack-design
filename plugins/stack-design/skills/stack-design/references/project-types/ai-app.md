# AI application

Clarify model provider, privacy, latency, token/media cost, evaluation, fallback, ingestion, and asynchronous work. Prefer the existing backend unless Python-only dependencies justify a worker or service. Start RAG with object storage, background jobs, PostgreSQL + pgvector, and measurable retrieval evaluation.
