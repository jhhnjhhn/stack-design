# E-commerce

Prioritize transactional integrity, inventory concurrency, idempotent payment/webhook handling, audit, security, and recovery. Use a relational source of truth. Add cache and search only where catalog scale or measured latency needs them. Prefer hosted payment boundaries to handling card data directly.
