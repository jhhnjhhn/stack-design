# Internal tool

Bias toward an authenticated SPA or server-rendered UI, modular monolith, relational database, and simple deployment. Optimize delivery and maintenance for the actual user count. Avoid SSR for SEO, microservices, Kafka, Kubernetes, and separate search unless another requirement independently justifies them.
