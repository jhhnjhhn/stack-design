# Observability

Introduce structured logs first, health checks and actionable alerts next, metrics when trends or SLOs matter, and tracing when distributed latency cannot be diagnosed otherwise. Define ownership, retention, privacy, sampling, and cost. A full platform is not a default requirement.
