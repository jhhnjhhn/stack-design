# Authentication

Prefer secure server sessions for a browser-only first-party app. Use OAuth/OIDC for delegated identity and enterprise SSO; JWT is a token format, not an authentication architecture. Managed auth accelerates delivery but adds vendor and pricing risk. Self-host only when data residency, customization, or organizational capability justifies its security burden.
