# Search

Progress from SQL `LIKE` to PostgreSQL full-text search. Add Meilisearch or Typesense when product-grade typo tolerance and relevance are required at moderate operational cost. Use Elasticsearch/OpenSearch only when advanced relevance, aggregations, or scale justify a cluster. Plan indexing consistency and rebuilds.
